// Indirection seam: the physical track swaps the target of this re-export (to an adapter
// over the real hook) without touching any consumer. See the header note in .stub.ts.
export * from './usePhysicalTsumego.stub';
