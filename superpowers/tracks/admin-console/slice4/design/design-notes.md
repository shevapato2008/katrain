# 训练与模型设计方向

- 本地 HTML：[训练与模型](./admin-vision-training.html)。claude-design 主 **Operate**、次 **Monitor**：320px 创建配置，右侧同 run 进度、指标和日志，下方只读模型版本。不采用营销 hero、等权功能卡或装饰图。
- 读取已有正式后台 CSS，保留楷体、B 夜间铜色/C 白天蓝灰、17px 内容/按钮和16px关键元信息；技术日志12px等宽，设计示例说明13px。独立评审要求重要说明对齐现有模块，并把次要参数折叠以让主按钮和版本行进入首屏。生产通用导航仍三模块，未集成的性能链接不冒充可用。
- ui-ux-pro-max 新页设计系统查询给了泛用蓝粉/Fira 及运营落地页建议；因与 Fan 已确认主题和中文楷体冲突，只采用密度8、状态反馈、显式标签、焦点、进度与禁用原因原则，保留现有系统而非替换成搜索建议。
- unknown 显示尚未读取、GPU 未核实、无真实数据与指标；其余三态均明确设计示例。未观测的验证指标是 `—`，不填0。运行时配置锁定且只允许取消本 run；失败保留旧模型，新运行不暗中重启。
- 首版单 GPU，不标称双卡 DDP。真实 GPU 预留、可信权重、固定根目录和磁盘待逐次授权探测；设计演示按钮仅切换本地状态，不连接服务器。
- claude-design slop 自检 **0/10**：无渐变/玻璃/装饰rail/巨大统计/模板tile/center stack；中文楷体是用户选择，构图按真实训练操作。
- 四态已在本地 Chromium 1440×900 预览。独立 Astra max 首轮只要求首屏 CTA/版本行、重要字号两处修订，折叠参数及紧凑布局后以完成态代表图确认：按钮底875px、版本行底890px，最终 **APPROVE**。后续 Fixture 四图通过前不写本切片后端；远端授权未取得。
