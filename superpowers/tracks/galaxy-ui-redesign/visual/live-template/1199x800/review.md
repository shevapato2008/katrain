# 1199×800 视觉审阅

- 日期：2026-08-06；真实页面：`/galaxy/live/yike_184016`；用户视觉确认：**待确认**。
- 自动几何：PASS（仅布局）。52px 顶栏；docked 左栏为 0，临时 Drawer 默认关闭；320px 右栏；棋盘 `728×728`，完整正方，水平溢出 0px。
- 构图/交互：左上 44px 菜单触发器可打开覆盖式左栏，不改变棋盘矩形；棋盘较 1200 断点突然增大 104px，这是按模式切换的预期结果。
- 层级/滚动：棋盘与右栏保持横排；右栏模块/中段/回放区固定结构正确，中段 575px。
- 字体/素材/颜色/状态：真实 logo、木纹、棋子、MUI icons 和直播数据保留；品牌、中文、Latin/数字字体边界不变。
- 与参考差异：参考中的 240px 常驻左栏被 overlay 取代，避免棋盘再次被压缩；右栏 500→320，中文导航和品牌更新。
- 保留问题：趋势图几乎被显示开关覆盖；320px 回放手数明显裁切；`COORDINATES` 仍英文。**视觉未通过，待确认/修正。**
