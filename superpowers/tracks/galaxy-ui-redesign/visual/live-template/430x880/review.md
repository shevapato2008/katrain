# 430×880 视觉审阅

- 日期：2026-08-06；真实页面：`/galaxy/live/yike_184016`；用户视觉确认：**已批准**。批准证据：用户查看修复版后明确回复“明白，没问题了”。
- 自动几何：PASS（仅布局）。52px 顶栏、无左栏/触发器、纵向 shell、64px 固定底栏；棋盘从参考图 `0×0` 恢复为 `410×410`，完整正方，水平溢出 0px；坐标开关因棋盘小于 500px 默认关闭。
- 构图：首屏依次为真实品牌顶栏、完整棋盘、模块牌开头；棋盘上下各约 16px/12px 内边距，木纹和棋子清晰。
- 层级/滚动：module、AI/趋势、两列开关、回放依次纵向排列。shell `scrollHeight=1391`、`clientHeight=828`、最大 `scrollTop=563`。
- 动作可达性：滚到底后动作区 bottom `816.02`、底栏 top `816`，`actionsUnobscured=true`；`implementation-scrolled.png` 显示完整趋势、开关、回放与固定底栏。
- 字体/素材/颜色：真实 `/assets/img/logo-white.png`、木纹/棋子/最后一手和 MUI icons 保留；品牌龙藏、中文文楷，Latin/数字系统回退；暗色容器与绿色动作一致。
- 文案/状态：真实直播、棋手、胜率、规则/贴目、AI pending、趋势与 194/194 手均可通过滚动访问。
- 与参考差异：旧 240px 常驻侧栏和被挤为 0px 的棋盘被完全移除；改用完整棋盘、纵向信息和底部导航。
- 修复复核：滚动证据中坐标、首页、更多均已中文化，趋势与 controls 无重叠，`194 / 194 手` 完整；首屏之后滚动访问 AI/动作是既定纵向构图。**自动几何 PASS 不等于用户视觉确认。**
- 已接受非阻塞差异：Galaxy 中文字体构建体积约 16.7MB；1440 首屏请求预算的精确值记录于根级 `font-budget.json`。本视口未记录其他已接受差异。
