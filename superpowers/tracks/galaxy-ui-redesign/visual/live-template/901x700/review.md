# 901×700 视觉审阅

- 日期：2026-08-06；真实页面：`/galaxy/live/yike_184016`；用户视觉确认：**待确认**。
- 自动几何：PASS（仅布局）。52px 顶栏、overlay 左栏默认关闭、320px 右栏；棋盘 `561×561`，完整正方，水平溢出 0px。
- 构图：是最窄横向布局之一，棋盘仍完整；模块标题及直播 chip 可见，subtitle 省略。
- 层级/滚动：中段仅 475px，趋势标题/指标与显示开关发生明显空间竞争；固定回放区仍位于底部。
- 字体/素材/颜色/状态：真实 logo、原棋盘/棋子、MUI icons 和真实直播数据保留；字体边界与其他中文页一致。
- 与参考差异：左栏不再常驻，右栏缩到 320px；棋盘从不可用的小尺寸恢复，顶栏/品牌/中文文案统一。
- 保留问题：趋势区指标与显示开关相互遮挡，图表不可读；回放手数裁切；`COORDINATES` 英文。**视觉未通过。**
