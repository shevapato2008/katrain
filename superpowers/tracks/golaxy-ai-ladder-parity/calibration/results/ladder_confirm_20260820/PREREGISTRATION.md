# 预声明 — ladder_confirm_20260820（单批 n=100 的承重边补 confirm）

开跑：2026-08-20 夜，写于开跑前。机器 home-ubuntu / katago-calib / humanv0。

## 为什么

自定规矩：**冻进 `ladder.py` 前每条承重边 ≥2 个独立批次、合并 n ≥ 200。**
级位段经三批已全部满足；**段位段与级段交界处还有 7 条只有单批 n=100**。
本批只做一件事：把它们各再跑一批 100 局，合并到 n=200。

复现有效性已实测：同 matchup 重跑进新目录是独立样本（同 (pair,color) 位胜负一致率 0.548
= p≈0.6 下的理论值 0.52；若同一盘棋应为 1.00），因为抽签由每批不同的 manifest_sha256 派生。

## 7 条缝（各 100 局）

1. `rank_1d@1` vs `rank_1k@1`      —— 1段←1级，级/段交界（现 0.680/n=100）
2. `rank_7d@1` vs `rank_5d@1`      —— 6段←5段（现 0.690/n=100）
3. `rank_7d@1s` vs `rank_7d@1t0.8` —— 7段←准7段（现 0.760/n=100）
4. `rank_7d@2` vs `rank_7d@1s`     —— 准8段←7段（现 0.640/n=100）
5. `rank_9d@1s` vs `rank_7d@2`     —— 8段←准8段（现 0.580/n=100，**全表最marginal的一条**）
6. `rank_9d@2` vs `rank_9d@1s`     —— 准9段←8段（现 0.660/n=100）
7. `rank_9d@4` vs `rank_9d@2`      —— 9段←准9段（现 0.750/n=100）

## 判据（写死）

- 合并后 <0.550 的边：该档从链上摘掉并如实上报，**不再补第三批**。
- 不重跑、不换配置、不动闸门。合并口径按 (A配置,B配置)，不按 seam id，不取最大值。
- 本批**不**为了让某条边过闸而选择性加 n：7 条一次全跑，结果一次全收。

---

⚠️ **本批漏了 `engine_health_prelaunch.json`**（同日其余三批都有）。这是流程疏忽，
**不事后补**——事后拍的快照不是开跑前快照，补上等于造证据。可用的旁证：本批与
同日 `ladder_kyu550_20260820`（有快照）跑在同一台机器、同一个 `katago-calib` 容器
（连续 up 7 天未重启）、同一份 humanv0 权重，中间没有任何部署动作。
