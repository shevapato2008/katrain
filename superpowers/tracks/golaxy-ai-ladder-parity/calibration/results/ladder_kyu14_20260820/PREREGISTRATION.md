# 预声明 — ladder_kyu14_20260820（补第 14 档 + 承重边 confirm）

开跑：2026-08-20 傍晚，写于开跑前，未看本批任何数据。
机器：home-ubuntu / katago-calib（TensorRT，8010）/ humanv0。闸 = **0.550 点估计（Fan 裁定）**。

## 背景

ladder_kyu550 之后，只认实测边的最长链是 **13 档**：
`20k→14k→12k→11k→10k→9k→8k→7k→6k→4k→3k→2k→1k`。
原计划的 `20k→18k`（拟合预测 0.567）实测 **0.490**，18级 按预声明出局、不重测。

第 14 档只可能插在 20k 与 14k 之间。重拟合后（31 条边）该区间是一片平台：
20k=0 / 19k=−3.7 / 18k=+10 / 17k=+54 / 16k=+45 / 15k=+82 / 14k=+144（Elo，拟合值，非证据）。

## 主选与备选（**在测之前定死，防选择效应**）

| 路线 | 边 1 | 边 2 | 较弱的一边 |
|---|---|---|---|
| **17k（主选）** | 20k→17k 预测 0.577 | 17k→14k 预测 0.629 | **0.577** |
| 15k（备选） | 20k→15k 预测 0.618 | 15k→14k 实测 0.567(n=120) | 0.567 |

**规则：按 maximin 取主选 = 17k 路线。只要 17k 路线两条边合并后都 ≥0.550，就采用 17k，
哪怕 15k 路线测出来的数字更高。** 只有 17k 路线掉边时才看 15k。两条路线都掉 ⇒ 级位段
就是 13 档，如实上报，**不再找第三条路线**（继续找等于对同一个假设反复抽签）。

## 本批 5 条缝，各 100 局（confirm/50 pair）

1. `rank_17k@1` vs `rank_20k@1`  —— 主选边 1（未测过）
2. `rank_14k@1` vs `rank_17k@1`  —— 主选边 2（未测过）
3. `rank_15k@1` vs `rank_20k@1`  —— 备选边 1（未测过）
4. `rank_14k@1` vs `rank_15k@1`  —— 备选边 2，把 n=120 补到 ~220
5. `rank_12k@1` vs `rank_14k@1`  —— **承重边 confirm**：现为 0.660/n=100 单批，
   按「冻进 ladder.py 前 ≥2 个独立批次、合并 n≥200」的规矩必须再来一批。

## 判据（写死）

- 合并口径：按 (A配置,B配置) 归一化方向合并全部批次，不按 seam id，不取最大值。
- 缝 5 若合并后 < 0.550：12级 从链上摘掉，链长降到 12 档 + 第 14 档候选，如实上报。
- 本批任何一条都不因为结果难看而重跑、补 n 或换配置。
- 闸门 0.550 不动。

---

⚠️ **本批漏了 `engine_health_prelaunch.json`**（同日其余三批都有）。这是流程疏忽，
**不事后补**——事后拍的快照不是开跑前快照，补上等于造证据。可用的旁证：本批与
同日 `ladder_kyu550_20260820`（有快照）跑在同一台机器、同一个 `katago-calib` 容器
（连续 up 7 天未重启）、同一份 humanv0 权重，中间没有任何部署动作。
