# kiosk-go-play-ai · 上板清单（RK3562）

> 盒子只有 2G 内存，一次只跑一家。先确认板上代码与本分支同一提交；板上没有 git 时，比对文件的 `git hash-object`。每项记录日期、板上提交、结果、耗时和截图路径。

| # | 条目 | 板上步骤 | 通过判据 | 日期 / 提交 / 结果 / 证据 |
|---|---|---|---|---|
| 1 | A12 数子补分 | 自由对弈，关掉「领地」「图表」，下满 100 手后点「数子」 | 先显示「正在数子…」，15 秒内出现 `黑+x` / `白+x` 结果卡；记录点击到结果的秒数 | 待上板 |
| 2 | N22 AI 跟停 | 已登录自由对弈，人先停一手、AI 跟停 | 结果卡给出胜负，而非只写「终局」；屏 01「全部对局」里出现这一局 | 待上板 |
| 3 | N22 升降级双停 | 升降级局人先停、AI 跟停 | 对局结束；回开局页没有挡局面板，「继续」按钮消失，能开下一局 | 待上板 |
| 4 | N21 AI 思考中认输 | 自由对弈人落子后立刻点「退出对局 → 认输并退出」 | 「全部对局」记为 AI 胜；回到这一局不再出现 AI 新落的子 | 待上板 |
| 5 | A18 读秒判负 | 自由对弈选「仅读秒 30秒×3」，轮到自己时不下 | 时钟从「读秒 · 剩 3 次」数到「超时」，结果卡为对方超时胜 | 待上板 |
| 6 | A20 改用屏幕落子 | 实体盘对局，故意不摆 AI 的子，等同步弹层后点「改用屏幕落子」 | 10 秒内不再出现「棋盘可能被移动」弹层，开关排没有「标定丢失」；屏幕点子可继续 | 待上板 |
| 7 | N17 失效的继续上一局 | 开一局后重启 KaTrain 服务，回屏 01 点「继续上一局」 | 对局屏显示「这一局已经打不开了」和「回到对弈」；回屏 01 后继续入口消失 | 待上板 |
| 8 | 终局后翻手 | 已登录自由对弈认输或双停结束，立刻上一手、最后一手 | 结果卡与打谱键始终在；屏 01 无继续入口；「全部对局」只有一局 | 待上板 |
| 9 | 断线出口 | 对局中重启服务，见断线红条后点「退出对局 → 先离开，不认输 → 继续上一局」；另测仅断连接而不重启 | 重启后见打不开与返回出口；全程无认输、无卡死；仅断连接时回来仍是同一局 | 待上板 |

## 视觉证据（待 Fan 确认）

屏 05 和屏 02 均有同尺寸 1024×600 的[四图存档](../kiosk-go-shell-align/visual/05-game/1024x600/05-game--side-by-side.png)与[屏 02 四图](../kiosk-go-shell-align/visual/02-setup-free/1024x600/02-setup-free--side-by-side.png)；各目录同时保存参考、实现和差异图。屏 05 的先前两次四图运行像素差为 0；屏 02 的四图已保存。现有差异包括棋盘材质与线条、末手标记、图表数据及开局说明文案；当前步骤只登记证据，不判视觉通过。

本轮状态截图：[失效续局](visual/n17-game-unavailable-1024x600.png)、[读秒局](visual/a18-timed-game-1024x600.png)、[实地策略说明](visual/a3-strategy-hint-territory-1024x600.png)、[升降级右栏](visual/n14-a11-ranked-rail-1024x600.png)、[本地对局右栏](visual/n14-a11-pvp-local-rail-1024x600.png)、[重标定](visual/a21-recalibration-1024x600.png)。屏 10 沿用受影响的既有浏览器测试。真机截图与 Fan 的确认结果在上板后填入上表。

## 本轮登记的后续项（非上板）

1. 本地对局连点「停一手」可能把两人的停都按掉；共享 `/api/move` 需带 `expected_node_id`，与 galaxy 一起验收。
2. galaxy 的超时尚未绑定局面轮次，前端也尚未读取 `terminal_result`；本轮服务端只冻结终局所在局面线，kiosk 前端整局冻结。
3. 视觉 orchestrator 缺终局暂停原因；结束后识别仍在跑，提交被拒再布防。
4. 大厅多人局可能重复落账；认输不清 `pending_count_request`，timeout 的胜方与盘面轮次可能不一致，终局后导航另开分支再认输可能再记一行。归人人对弈模块。
5. 本地认输框点名方来自浏览器，请求未绑定该方。
6. 服务端翻手期间不计时，盒上服务重启后计时状态丢失；本轮保持现有语义。
