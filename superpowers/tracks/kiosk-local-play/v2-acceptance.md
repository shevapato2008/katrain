# kiosk 本地对局 v2 验收记录

日期：2026-09-16  
设备：RK3562 `rk3562-p04-001`  
最终验收提交：`5ff20a96575242d08870afe2717ce729ac112f0b`  
隔离目录：`/mnt/data/katrain-kiosk-local-play-5ff20a96/`  
隔离端口：`18082`  
部署包 SHA-256：`706aaa61ccc94f59dac927fdf62a37cb1c3dc7d7503eac66112ca4402674ae63`

## 结论

本地对局 v2 的主旅程和 P1–P20 已验收。严格 kiosk 2D 包在独立 HOME、独立 SQLite 和独立端口上运行；终验完成后停止隔离进程，保留部署副本。正式 `smartbox-katrain` 与 `smartbox-katago-api` 均已恢复为 `active`，18082 已释放。

隔离实例没有配置 `KATRAIN_REMOTE_URL`，因此屏 19 能正确展示本机历史、报告档位和错误态，但真正提交远程报告会如实返回 `503 Remote report service unavailable`。本轮没有借用生产账号或向远程服务写入数据；屏 19 到研究页的本地 SGF 交接已走通。

## RK3562 旅程

| 项目 | 结果 | 证据 |
|---|---|---|
| 屏幕开局与落子 | 通过 | 19 路本地对局开局，点击棋盘后由第 1 手进入第 2 手。 |
| 双 pass 自动数子 | 通过 | 双方停一手后进入“正在数子”，得到终局结果并落账。 |
| 手动数子 | 通过 | 9 路局走到第 22 手后按钮可用，结果 `B+18.6`。 |
| 9 路 | 通过 | 设置页显示“数子要下满 22 手”，全程走屏幕落子，没有被实体盘守卫拦截。 |
| 让子 | 通过 | 让 2 子时贴目为 0，白方先行，摘要显示“让 2 子”。 |
| 认输选方 | 通过 | 对话框同时提供黑方、白方；选择黑方后权威结果为 `W+R`。 |
| 退出不保存 | 通过 | 未终局退出显示“不保存”；会话删除，`user_games` 数量不增加。 |
| 超时判负 | 通过 | 1 秒读秒局得到 `W+T`；终局后点击棋盘，棋子与结果均未变化。 |
| 屏 19 | 通过 | 显示 5 局本机历史；“本地对局 · 未记名”、超时、认输和数子结果正确。 |
| 报告 | 有边界地通过 | 报告入口、500/2000 档位和重试态正确；隔离实例未配置远程报告服务，提交返回明确 503。 |
| 去研究 | 通过 | 终局 SGF 进入 `/kiosk/research?from=game`，显示“刚下完的这一局”和真实第 1 手盘面。 |
| 实体盘开局 | 通过 | 设置页“实体盘”可选，活动会话写入 `onBoard: true`；沿用现有标定后 `pose_locked=true`、`sync_state=synced`。 |
| 相机与识别 | 通过 | RKNN、相机、几何和识别均 ready；日志持续输出真实棋子识别与 board delta。 |
| 落子灯、拿除灯 | 通过 | LED 串口 connected；中心点分别提交 `black`、`remove`，两次均返回 `ok:true`，随后清灯，日志为 1→0、1→0。 |
| 最终文案回归 | 通过 | 最终提交的实际终局徽章显示 `W+1.9子`，页面不含 `result:points_*`。 |

## P1–P20

| 条目 | 状态 | 验收说明 |
|---|---|---|
| P1 | **已修** | 本地对局到点会调用权威 `/api/timeout`；板端得到 `W+T`。 |
| P2 | **已修** | 玩家卡显示主时间/读秒三态；超时状态条写明胜负与手数。 |
| P3 | **已修** | 让子局请求发送 `komi: 0`；板端让 2 子摘要与白方先行一致。 |
| P4 | **已修** | 双 pass 先进入 `awaiting_count`，数子成功后才以真实胜负落账。 |
| P5 | **已修** | “退出”不再等于认输；认输先选择黑方或白方，退出未终局局不保存。 |
| P6 | **已修** | 数子失败按 `below_min_moves`、`game_over`、`analysis_pending` 等原因说真话。 |
| P7 | **已修** | 数子门槛随路数缩放；9 路为 22 手，已在板端手动数子。 |
| P8 | **已修** | 开局时把 `onBoard` 写入活动会话；9/13 路自动走屏幕且不会被标定守卫拦截。 |
| P9 | **已修** | 对局提示音与设置页统一读写 `audioPrefs.sfx`。 |
| P10 | **已修** | 本地对局不提供 AI 支招/形势判断；右栏只保留本模式成立的操作。 |
| P11 | **已修** | 设置页明确说明双方停一手后由引擎判断并自动数子，不再声称有手工死子确认。 |
| P12 | **已修** | 本地对局空名字不回填登录用户名；屏 19 显示“未记名”。 |
| P13 | **已修** | 研究页不再以 `!token` 阻断盒端 cookie 身份；终局 SGF 到研究页已通过。 |
| P14 | **已修** | “未终局”只用于未完成的对弈；导入、棋谱库和研究存档没有结果时仍可生成报告。 |
| P15 | **已修** | 本地局两个名字都空显示“本地对局 · 未记名”，有名字时显示“两人”。 |
| P16 | **已修** | 首页平台状态按已登录状态请求，不再要求内存 token。 |
| P17 | **已修** | 屏 19 首帧 loading 按已登录状态初始化，不再因盒端 token 为空闪空态。 |
| P18 | **已修** | `/api/count/request` 使用与认输、超时相同的会话终止权限校验。 |
| P19 | **已修** | 本轮文案已在 11 种语言补齐；终验发现的数子单位缺省值也已修复并加回归测试。 |
| P20 | **已修** | 实体盘沿用标定、会话绑定、相机识别、落子灯和拿除灯均在 RK3562 实测。 |

## 自动化与构建

- 后端本地对局目标测试：58 passed。
- 前端本地对局和报告呈现测试通过；本次终验新增的结果单位测试与报告呈现测试共 26 passed。
- TypeScript `--noEmit` 与修改文件 ESLint 通过。
- `npm run build:kiosk-2d` 通过，严格边界检查确认 14 MB、214 个文件，未带 three.js、`/galaxy/` 或非盒端 live API。
- 全量 Vitest 首轮有 1 个既有 Kifu 时序波动；该文件单独复跑 20 passed。本轮没有为低风险文案修复扩张测试范围。
- 浏览器套件与本地对局相关的 10 项通过；2 项依赖完整后端/资产的非本地对局用例未纳入本轮修复。

## 部署与恢复

- 初次终验基线：`b4c011ec51d515357c6ddeaac8c863031e20f44b`，隔离目录 `/mnt/data/katrain-kiosk-local-play-b4c011ec/`。
- 终验发现结果卡片在实时翻译不可用时显示 `result:points_zi`，修复提交为 `5ff20a96575242d08870afe2717ce729ac112f0b`。
- Fan 确认新 SHA 与新目录后重新部署；最终浏览器复验显示 `W+1.9子`。
- 两个隔离部署目录均未覆盖 `/root/smartbox-software/vendor/katrain`；正式服务仅在硬件资源终验期间临时停止，随后恢复。
