# Review Response — accept/reject decisions + plan fix-list

Every code-dependent claim was verified against the real repo (evidence in `review-feedback-*.md` cross-checked by a verification pass). Decisions below; the **Fix** lines are the exact edits applied to `plan.md`.

## Refuted / pushed back

- **Gemini MAJOR — "Board.tsx needs new props for eval color dots; Phase C is an unacknowledged shared edit."** REFUTED. `components/Board.tsx:24-31` defines `EVAL_COLORS`; `:228-248` renders candidate circles from `gameState.analysis.moves` under `analysisToggles.hints`; `:324-334` renders per-move eval dots under `analysisToggles.eval`. `BoardProps` (`:5-11`) takes no analysis-specific props. Galaxy's report already drives it this way. **Board is reused as-is; no shared edit for eval rendering.** (The one genuine shared-capability gap Gemini/Codex both circle — dead-stone red-X — is handled by descope, below.)
- **Gemini — "B6 Settings missing calibration multi-step / failure-diagnosis states."** MODIFIED. `GeometryCalibrationWorkspace`'s existing phase titles + diagnostic/failure card are reskinned by **A13** (Phase A), not B6. The genuinely-new multi-step progress UI (清空/暗参考/角点/校验/建基线) is `design.md §5.8 待补` = future, out of scope. **Fix:** add a note to A13 clarifying existing surfaces are reskinned, new multi-step states are deferred.
- **Gemini — "B7 摆谱 missing DriftBanner / capture-fail / resume-undo-exit / disabled states."** PARTIAL. Those surfaces **already exist** in `BaipuSessionPage`; the reskin must retheme them + preserve testids (not build them new). **Fix:** B7 gets an explicit step to reskin the existing `DriftBanner`, capture-error banner, resume/undo/exit confirm dialogs, and capture-`disabled` advisory. Truly-unbuilt 待补 states stay out of scope.

## Accepted — correctness (verified CONFIRMED)

1. **Dead-stone red-X, game state C (Codex #8).** `Board.tsx:187-200` draws ownership as translucent squares only; no dead-stone opacity/red-X; `api.ts:23 analysis:any` exposes no `dead` field. **Fix (B1.4):** descope 死子淡化+红叉 for this cut. State C = territory coloring via `analysisToggles.ownership` + result card + score breakdown. Add an explicit note: dead-stone red-X needs a backend `dead_stones` field + a kiosk-only overlay (Gate S) — deferred.
2. **`继续对弈` dead action (Codex #7).** `useGameSession.ts:126-152` has no `resume` branch (unmatched → silent no-op). **Fix (B1.4):** `继续对弈` dismisses the result overlay via **local component state** (game stays ended); do NOT call `handleAction('resume')`. `确认终局` navigates out.
3. **AI-thinking misclassification (Codex #5).** `player_type` is `player:human`/`player:ai` (kiosk HvAI, `server.py:723/727`) OR bare `human` (multiplayer, `session.py:80/82`); tests use bare `ai`. **Fix (B1.3):** replace the `firstHumanColor` heuristic with a per-color derivation that accepts BOTH literals:
   ```ts
   const isAI = (c: 'B' | 'W') => { const pt = gameState.players_info[c].player_type; return pt === 'player:ai' || pt === 'ai'; };
   const aiColor = isAI('B') ? 'B' : isAI('W') ? 'W' : null;
   const aiThinking = !!aiColor && gameState.player_to_move === aiColor && !gameState.end_result;
   ```
   The persistent AI-move banner and state A are gated on `aiColor !== null`. Add tests for an AI game AND a both-human PVP game (no "AI思考中").
4. **Guard blocks screen-only tsumego (Codex #2).** `PhysicalBoardGuard.tsx:7-10` — with `requireRecognition`, a phase==='ready' camera kiosk without `recognition_ready` is blocked; `KioskApp.tsx:69` wires `requireRecognition` on the tsumego route. **Fix (B2.5):** remove `requireRecognition` from the `tsumego/problem/:problemId` route (screen solving is the default; physical mode owns recognition when toggled on). Add a guard test for `phase:'ready', geometry_ready:true, recognition_ready:false` (renders content). Note the same guard is on `research/session/:sessionId` (kifu-open target) — see fix 20.
5. **Board-loss dialog stacking (Codex #6).** `VisionSyncOverlay.tsx:95,278-302` owns `boardLostOpen` internally; props (`:36-42`) have no suppression. `RecalibrationModal` does not exist yet (it is the proposed B1.4 component). **Fix (B1.4):** add a `suppressBoardLost?: boolean` prop to the kiosk-owned `VisionSyncOverlay` and pass `suppressBoardLost={escalationOpen || recalOpen}` from GamePage; single-modal arbiter priority escalation > board-lost > recalibration. Add a test asserting board_lost + escalation cannot both render.
6. **kifu `?analyze=1` analyzes empty board (Codex #4).** `galaxy/ResearchPage.tsx:187-199` — effect dep `[searchParams]` captures `handleStartAnalysis` from the empty-board render; `serializeToSGF` reads stale `moves` state → `createSession(undefined,...)`. Deterministic, not timing. **Fix (C1.6 Step 9):** do NOT port verbatim. For the kiosk deep-link, serialize the just-fetched `album.sgf_content` and pass it directly into `createSession` (a `startAnalysisFromSgf(sgf, initialMove)` path), OR drop the `?analyze=1` auto-start entirely (open into the editor). Add a C1.7 test: `?kifu_id=1&analyze=1` asserts `createSession` receives the album SGF, not `undefined`.
7. **C1.6 unmount cleanup stale closure (Codex #9).** `useResearchSession.ts:97-105` gates DELETE on `base.sessionId`; a `[]`-dep cleanup captures `sessionId===null`. Galaxy's real code avoids this (`ResearchPage.tsx:165-174`, deps `[session.sessionId]` + keepalive fetch). **Fix (C1.6 Step 3):** mirror galaxy — cleanup effect deps `[session.sessionId]`, or read a `sessionIdRef` inside the cleanup and use `fetch(DELETE, {keepalive:true})`. Never a `[]`-dep `destroySession`.
8. **Research board-size 19-only (Codex #13).** `useResearchBoard.ts:213` `if (metadata.boardSize) setBoardSize(metadata.boardSize)`; `loadFromSGF` reachable from file-open/library/`?kifu_id`. **Fix (C1.2 + C1.6):** in the kiosk port, after any `loadFromSGF`, force `boardSize` back to 19 (kiosk physical board is 19-only); if a non-19 SGF is imported, show a toast "仅支持 19 路" and clamp. Komi/handicap/rules from the SGF are still honored.
9. **Scan resume keeps in-memory state (both, CONFIRMED + caveat).** `interface.py:798-827` `_do_analysis_scan` queues only `not node.analysis_exists`, so re-calling `analysisScan` resumes incrementally — BUT `analysis_exists` is in-memory session state. **Fix (C1.6 state D):** analysis-failure/retry re-calls `analysisScan` on the SAME live session; do NOT `destroySession` on failure (that restarts from 0). Only destroy on 返回编辑.
10. **C1.6 undeclared state/refs (Codex #3).** Steps 4/6 use `setEtaSeconds`, `analysisStartRef`, `hintsEnabledRef` never declared. **Fix (C1.6 Step 2):** add
    ```ts
    const analysisStartRef = useRef<{ time: number; analyzed: number } | null>(null);
    const hintsEnabledRef = useRef(false);
    const [etaSeconds, setEtaSeconds] = useState<number | null>(null);
    ```
11. **B6.3 does not compile (Codex #10).** Uses `t(...)` without importing `useTranslation`. **Fix (B6.3):** add `import { useTranslation } from '../../../hooks/useTranslation';` + `const { t } = useTranslation();`, and add a render test (not build-only).
12. **B2.5 ↔ D1.3 physical wiring conflict (Codex #1).** Both declare a `physicalMode` state; D1.3 test expects `data-phase="clearing"` but D1.2 maps `clearing`→`null`. **Fix:** B2.5 introduces the `physicalMode` state + `PhysicalModeToggle` ONCE. **D1.3 consumes B2.5's state** (does not re-declare/re-read). Resolve the render contract: `enable()` sets phase to `setup` (a visible state); the D1.3 test asserts `data-phase="setup"` after enable, and `clearing`/`clearing_next` render `null` (transitional). PhysicalStatePanel renders only for `setup|ready|replying|removing|solved`.
13. **B2.5 duplicates A11 immersive wiring (Codex #12).** A11 already adds the `useImmersive` import + mount effect to `TsumegoProblemPage`. **Fix (B2.5 Step 1):** "verify/preserve the A11 immersive effect" — do not re-add.
14. **MUI icon testid suffix (Codex #11).** `@mui/icons-material ^7` renders `data-testid="{Name}Icon"`. **Fix (B2.1 + all MUI-icon testid assertions):** assert `data-testid="EmojiEventsIcon"` (name + `Icon`), or set an explicit `data-testid` on the icon and assert that.

## Accepted — sequencing / boundary / hygiene

15. **C1.6/C1.7 must land atomically (Codex).** **Fix:** C1.6's gate is build+lint only; C1.7 (the rewritten test) lands in the same commit/immediately after — C1.6 cannot pass a vitest gate against a test it hasn't written. State this in both tasks.
16. **B6.2 logout redirect (Codex).** A12 contract requires `navigate('/kiosk/login', { replace: true })`. **Fix (B6.2):** add `{ replace: true }`.
17. **Gate S wording (Codex).** The matrix currently says Gate S = "edits **or newly consumes** shared territory", but many tasks newly consume an already-bundled shared component and correctly claim Gate K. **Fix (header):** redefine **Gate S = any EDIT to a shared-territory file**. Newly *consuming* an already-kiosk-bundled shared file is Gate K (note it). Re-mark A9 (consumes LiveBoard), B6.1/B6.2 (consume Settings/Auth context), B2.2/B1.1 (consume activeSession) as Gate K.
18. **Emoji gate too narrow (Codex).** `[\x{1F000}-\x{1FAFF}\x{2600}-\x{27BF}]` misses transport glyphs ⏮(23EE) ◀(25C0) ▶(25B6) ⏭(23ED). **Fix (header Gate E + every Gate-E step):** broaden to `grep -rP '[\x{2190}-\x{27BF}\x{2B00}-\x{2BFF}\x{1F000}-\x{1FAFF}\x{FE0F}]' src/kiosk` (covers arrows, misc-technical media controls, geometric shapes, dingbats, emoji, variation selectors).
19. **D1.1 factual note (Codex).** `api/ledApi.ts` **exists** (used by Baipu). **Fix (D1.1):** correct the note to "reuse existing `api/ledApi.ts`"; the stub still must not call it (side-effect-free).
20. **Route coverage (Codex).** Verified: only `play/pvp/setup` (`KioskApp:62`) and `kifu/:kifuId` (`KioskApp:77`) still resolve to `PlaceholderPage`. `LobbyPage`/`PlatformConnectPage`/`PlatformLobbyPage` are **real, user-visible** (326/300/224 lines, linked from PlayPage). **Fix (Phase B):**
    - Add **Task B1.6** — reskin `LobbyPage`, `PlatformConnectPage`, `PlatformLobbyPage`, `PlatformEngineSetupPage` to slate tokens (they're linked from the hub; play map already noted them). Gate K + emoji/hex gates + preserve tests.
    - `play/pvp/setup`: point the route at the **AiSetupPage skeleton** (design §5.1 "复用设置页骨架") OR mark the 本地对局 hub card 敬请期待 — B1.1 decides; add the disposition explicitly.
    - `kifu/:kifuId`: confirmed unused (kifu opens via research) — leave `PlaceholderPage` but note it in B3 as intentionally-unused (no scaffolding).
21. **Physical stub must match the REAL hook (Gemini-CRITICAL-D, Codex §7.8).** The sibling `../katrain-kiosk-physical-tsumego` hook is built but its signature ≠ the plan's invented contract. **Fix (D1):** D1.1 Step 1 = read the real `usePhysicalTsumego.ts` in the sibling worktree and **adopt its exact exported signature** as the stub's interface (the stub is an adapter conforming to the real hook, so the swap is drop-in). If the real signature diverges from the plan's `PhysicalTsumegoState`, the real one wins; update PhysicalStatePanel to it.

## Accepted — small / labels

22. **A3 checklist source (Codex §7.1):** note "regenerate the consumer/test list from `rg 'isPortrait' src` at execution time" (line lists are hints).
23. **SmartBoardConsole label (Codex §7.3):** the static preview must show a "实时预览暂不可用 / no live feed" label so it isn't mistaken for recognized-board data.
24. **Header engine dot (Codex §7.4):** label/aria the static engine dot as "assumed-ready" (real engine-health wiring is future); keep camera/calibration live.
25. **Phase C overflow check (Gemini):** add to C1.3/C1.4/C1.6 a runtime step "verify no overflow/clip on a 1024×600 viewport (340px L1 rail / 404px report rail + board)".
26. **KioskLayout immersive test (Codex §7.2):** A10 adds an integration test where an `<Outlet/>` child calls `setImmersive(true)` and asserts Header + Dock disappear.
